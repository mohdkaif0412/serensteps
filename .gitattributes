# Keep container scripts LF even when checked out on Windows — a CRLF
# shebang line makes the entrypoint unrunnable inside the image.
*.sh text eol=lf
Dockerfile text eol=lf
docker-entrypoint.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
